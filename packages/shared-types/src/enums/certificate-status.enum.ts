export enum CertificateStatus {
  ACTIVE = 'active',
  TRANSFERRED = 'transferred',
  REVOKED = 'revoked',
}

export enum AnchorStatus {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  FAILED = 'failed',
}
